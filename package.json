{
  "name": "octergo-platform",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:packages": "pnpm --filter @octergo/shared --filter @octergo/config build && pnpm db:generate && pnpm --filter @octergo/database build",
    "dev": "pnpm build:packages && dotenv -e .env -- pnpm --parallel dev",
    "dev:web": "dotenv -e .env -- pnpm --filter web dev",
    "dev:api": "dotenv -e .env -- pnpm --filter api dev",
    "dev:bot": "dotenv -e .env -- pnpm --filter bot dev",
    "db:check": "dotenv -e .env -- tsx scripts/check-db.ts",
    "db:migrate": "dotenv -e .env -- pnpm --filter @octergo/database exec prisma migrate dev",
    "db:generate": "dotenv -e .env -- pnpm --filter @octergo/database exec prisma generate",
    "db:seed": "dotenv -e .env -- pnpm --filter @octergo/database exec prisma db seed",
    "bot:deploy-commands": "dotenv -e .env -- tsx scripts/deploy-commands.ts",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "discord.js": "^14.21.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
